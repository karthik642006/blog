[tool.poetry]
name = "blog"
version = "0.1.0"
description = "Lightweight checks for static blog assets"
authors = ["Codex <codex@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
